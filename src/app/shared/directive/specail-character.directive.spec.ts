import { SpecialCharacterDirective } from './specail-character.directive';

describe('SpecailCharacterDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecialCharacterDirective();
    expect(directive).toBeTruthy();
  });
});
